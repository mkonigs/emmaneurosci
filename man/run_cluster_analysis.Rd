% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_cluster_analysis.R
\name{run_cluster_analysis}
\alias{run_cluster_analysis}
\title{Run cluster analysis on cognitive domain profiles}
\usage{
run_cluster_analysis(
  data,
  vars,
  id_var = "subj",
  scaling = c("unscaled", "scaled"),
  method = c("umap", "regular"),
  k = 4,
  seed = 123,
  colors = c("skyblue", "palegreen", "orange", "tomato", "purple", "gold"),
  save_figures = FALSE,
  figures_dir = "figures",
  save_data = FALSE,
  data_dir = "databases",
  file_tag = "cluster_analysis"
)
}
\arguments{
\item{data}{\code{data.frame} containing the id column and all clustering
variables. Should already be filtered to the desired time-point / group
(e.g. baseline only).}

\item{vars}{Character vector of column names to cluster on (e.g.
\code{c("d_proc", "d_att_ctrl", "d_mem", "d_ver_wm", "d_vis_wm", "d_visuom")}).}

\item{id_var}{Name of the subject/id column. Default \code{"subj"}.}

\item{scaling}{\code{"unscaled"} (default) leaves scores as-is; \code{"scaled"}
row-centres each participant's scores by subtracting the row mean.}

\item{method}{\code{"umap"} (default) embeds the data with UMAP before
clustering; \code{"regular"} clusters directly on \code{vars}.}

\item{k}{Number of clusters (2–6). Default \code{4}.}

\item{seed}{Integer random seed for reproducibility. Default \code{123}.}

\item{colors}{Character vector of fill colours, one per cluster.
Defaults to \code{c("skyblue", "palegreen", "orange", "tomato", "purple", "gold")}.}

\item{save_figures}{Logical; write per-cluster and combined PDFs to
\code{figures_dir}. Default \code{FALSE}.}

\item{figures_dir}{Directory for saved figures. Created if absent.
Default \code{"figures"}.}

\item{save_data}{Logical; write the clustered dataset as a \code{.csv2} file
to \code{data_dir}. Default \code{FALSE}.}

\item{data_dir}{Directory for saved data. Created if absent.
Default \code{"databases"}.}

\item{file_tag}{Prefix used in output filenames. Default \code{"cluster_analysis"}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{data}}{\code{data.frame} with id column, all \code{vars}, and a \code{groups}
column containing the cluster assignment (integer, 1–k).}
\item{\code{cluster_model}}{The \code{kmeans} model object.}
\item{\code{nbclust_plot}}{WSS elbow plot (\code{ggplot}) to help choose \code{k}.}
\item{\code{dendrogram}}{Hierarchical clustering dendrogram (\code{ggplot}).}
\item{\code{group_plots}}{Named list (\code{"group_1"}, \code{"group_2"}, …) of
per-cluster bar plots comparing each cluster against all others.}
\item{\code{combined_plot}}{A \code{gtable}/grob of all group plots arranged in a
grid. Draw with \code{grid::grid.draw(res$combined_plot)}.}
\item{\code{stats}}{List with two elements: \code{p_values} and \code{cohen_d}, each a
named list (one entry per cluster) of per-variable statistics.}
}
}
\description{
Performs k-means clustering on a set of cognitive domain variables using
either raw variables ("regular") or a UMAP embedding ("umap"). Produces
an elbow plot, a dendrogram, per-cluster bar plots with t-test p-values
and Cohen's d, and a combined grid figure. Returns all outputs as a list,
with optional saving of figures and the clustered dataset to disk.
}
\examples{
\dontrun{
library(lubridate)

dat <- read.csv2("et_domains_normed_peds_COGCOR_age_2026-05-12.csv",
                  stringsAsFactors = FALSE, sep = ";")
dat$test <- ymd(dat$test)
data_T1 <- dat[dat$group == 1, ]

domain_vars <- c("d_proc", "d_att_ctrl", "d_mem",
                  "d_ver_wm", "d_vis_wm", "d_visuom")

res <- run_cluster_analysis(
  data         = data_T1,
  vars         = domain_vars,
  scaling      = "unscaled",
  method       = "umap",
  k            = 4,
  save_figures = TRUE,
  save_data    = TRUE
)

head(res$data)                      # clustered dataset
res$nbclust_plot                    # elbow plot
res$dendrogram                      # dendrogram
res$group_plots$group_1             # bar plot: cluster 1 vs rest
grid::grid.draw(res$combined_plot)  # all clusters combined
}

}
